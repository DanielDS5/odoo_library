<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="library_loan_action" model="ir.actions.act_window">
        <field name="name">Loans</field>
        <field name="res_model">library.loan</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Tree View -->
    <record id="library_loan_view_tree" model="ir.ui.view">
        <field name="name">library.loan.tree</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <tree string="Préstamos" decoration-success="status=='returned'" decoration-danger="status=='pending'">
                <field name="book_id"/>
                <field name="reader_id"/>
                <field name="loan_date"/>
                <field name="return_date"/>
                <field name="status"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="library_loan_view_form" model="ir.ui.view">
        <field name="name">library.loan.form</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <form string="Préstamos">
                <header>
                    <button name="action_pending" type="object" string="Pendiente" status="draft"/>
                    <button name="action_return" type="object" string="Devuelto" status="pending"/>
                    <field name="status" widget="statusbar" statusbar_visible="draft,pending,returned"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="book_id"/>
                            <field name="reader_id"/>
                        </group>
                        <group>
                            <field name="loan_date"/>
                            <field name="return_date"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>